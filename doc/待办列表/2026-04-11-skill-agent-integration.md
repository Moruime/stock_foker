# 2026-04-11 同花顺 Skill 接入三大 Agent - 待办列表

## 1 遗留问题

| 问题 | 优先级 | 说明 |
| --- | --- | --- |
| 副图指标面板未实现 | 中 | MACD/KDJ/RSI 数值已传至前端，独立子图待实现 |
| Agent 全链路响应较慢 | 中 | 新增同花顺 API 调用后响应时间可能进一步增加 |
| 缺少大盘整体数据看板 | 中 | 宏观环境页有 LLM 分析结论，但缺直观数字看板 |
| 缺少产业链关联分析 | 中 | 未涉及上下游公司情报 |
| 历史关注列表无前端入口 | 低 | GET /api/focus/history 已实现，前端暂无展示入口 |
| 快照面板需手动刷新日期列表 | 低 | Agent 刷新后快照面板不会自动更新日期列表 |
| .pids/ 旧日志文件残留 | 低 | logs/ 目录已接管，.pids/ 下旧文件可清理 |
| 同花顺 API 数据格式未验证 | 低 | chunks\_info/datas 返回格式待实测确认 |

## 2 待开发事项

### 2.1 第三阶段功能（产品设计文档 5.3 节）

| 功能 | 章节 | 说明 |
| --- | --- | --- |
| 智能选股推荐 | 2.9 | 基于画像筛选标的，覆盖个股和 ETF |
| 复盘模块 | 2.10 | 单笔复盘 + 周期性报告（周报/月报） |
| 回测功能 | 2.11 | 策略历史数据回测 |
| 通知与提醒 | 2.12 | 价格触达、公告、指标信号、止盈止损提醒 |

### 2.2 第二阶段剩余功能

| 功能 | 章节 | 说明 |
| --- | --- | --- |
| 对比基准分析 | 2.2.3 | 个股 vs 大盘/板块对比 |
| 分时走势图 | 2.2.1 | 交易时段分时数据 |
| 换手率/量比/主力资金流向 | 2.2.2 | 量价类补充指标 |
| 止盈止损触达提醒 | 2.7.2 | 价格监控 + 主动通知 |
| 风险预警 | 2.7.3 | 连续亏损冷静期、异常波动预警 |
| 大盘数据看板 | 2.5 | 大盘 K 线、涨跌家数分布、成交额、北向资金等 |
| 产业链关联分析 | - | 分析上下游公司、供应商、客户、竞争对手关系 |

### 2.3 数据能力建设

| 事项 | 优先级 | 说明 |
| --- | --- | --- |
| SectorAgent 接入 hithink-industry-query | 中 | 补充行业 PE/PB/ROE 量化估值数据 |
| 验证同花顺 API 实际返回格式 | 高 | 上线后需确认 chunks\_info/datas 数据结构 |
| 考虑 hithink-market-query 接入 | 低 | 板块主力资金流向数据 |

## 3 优化建议

### 3.1 性能优化

- Agent 响应时间：同花顺 API 新增约 2-4 次 HTTP 调用，可考虑并行化
- SSE 流式返回：先展示已完成 Agent 结果，再展示综合建议
- LLM 调用优化：基本面数据过长时需截断，避免超出 token 限制

### 3.2 工程质量

- 自动化测试：当前无测试覆盖，需补充 Agent 单元测试和 API 集成测试
- 错误监控：Agent 错误目前仅记录在日志和缓存表，缺少聚合监控
- TypeScript 严格模式：部分 Agent 返回数据使用 `Record<string, unknown>`，可细化类型

### 3.3 功能增强

- localStorage 持久化缓存：应用刷新后可恢复当日缓存
- 多模型切换：Settings 页直接切换不同 LLM 模型，无需编辑 .env
- Prompt 版本管理：Agent Prompt 模板可外置为配置文件，便于迭代优化
- SnapshotPanel 与 Agent 刷新联动：刷新 Agent 后自动重载日期列表

## 4 后续计划

1. 实测同花顺 API 返回数据格式，按需调整 Prompt 中的数据解析说明
2. SectorAgent 接入 `hithink-industry-query`，补齐行业估值数据
3. 优先补充副图指标面板（MACD/KDJ/RSI 独立子图）
4. 评估 Agent 响应时间优化方案（同花顺 API 并行化 vs SSE 流式）
5. 补充核心路径自动化测试
6. 启动第三阶段功能规划
