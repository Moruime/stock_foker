# 新增迭代记录与待办列表文件夹

## Context

Phase 2 Agent 链路已完成开发（从 LLM 抽象层到 4 个 Agent、前端 4 个新页面、增强版行情分析），期间还修复了多个 LLM 集成问题、更新了全部文档、迁移了日志目录。需要建立迭代记录和待办列表机制来追踪开发进展。

## 方案

### 1. 创建文件夹结构

```
doc/
├── 迭代记录/
│   └── 2026-04-06-phase2-agent.md
└── 待办列表/
    └── 2026-04-06-phase2-agent.md
```

两个文件夹下的文件一一对应，命名格式 `YYYY-MM-DD-简短主题.md`，日期按 8:00 AM 更新规则确定。

### 2. 迭代记录内容（精简）

文件：`doc/迭代记录/2026-04-06-phase2-agent.md`

包含以下部分：
- **开发概述**：一句话总结
- **新增功能**：表格列出（功能 | 说明）
  - Agent 链路后端（4 个 Agent + 数据抓取 + API 路由 + 缓存）
  - Agent 链路前端（4 个新页面 + 增强版行情分析 + 类型/API 封装）
  - LLM 抽象层（OpenAI 兼容 API、热重载配置）
- **问题修复**：简要列表
  - LLM 单例缓存导致配置不生效
  - Agent 降级结果缓存导致 LLM 可用后仍显示不可用
  - Qwen3 thinking 模式超时（新增 LLM_ENABLE_THINKING 配置）
  - 板块成分股代码缺失
  - AI 设置页 enable_thinking 字段未同步
  - AKShare 函数名变更（stock_hsgt_north_net_flow_in_em → stock_hsgt_hist_em）
- **工程改进**：简要列表
  - 3 个文档全面更新（技术架构、MVP 实现说明、产品设计）
  - 日志从 .pids/ 迁移到 logs/ 目录
- **关键文件变更**：列出新增/修改的核心文件

### 3. 待办列表内容

文件：`doc/待办列表/2026-04-06-phase2-agent.md`

包含以下部分：
- **遗留问题**：当前已知但未解决的问题
  - .pids/ 下旧日志文件需手动清理
  - 副图指标面板（MACD/KDJ/RSI 独立子图）前端未实现
  - 历史关注列表前端无展示入口
- **待开发事项（第三阶段）**：产品设计文档中未实现的功能
  - 智能选股、复盘、回测、通知提醒等
- **优化建议**：可改进项
  - Agent 全链路响应时间优化
  - 自动化测试覆盖
  - Agent 缓存策略细化

### 4. 文档风格

- 遵循项目现有中文技术文档风格
- 使用层级标题 `## 1` / `### 1.1`
- 善用表格和列表
- 精简务实，不追求篇幅

## 涉及文件

| 操作 | 文件 |
|------|------|
| 新建 | `doc/迭代记录/2026-04-06-phase2-agent.md` |
| 新建 | `doc/待办列表/2026-04-06-phase2-agent.md` |

## 验证

- 确认两个文件夹和文件创建成功
- 确认文件内容与实际开发成果一致
- 确认 markdown 格式正确
